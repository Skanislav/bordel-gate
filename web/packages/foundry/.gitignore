# Compiler artifacts
cache/
out/

# Foundry-installed dependencies (pinned via foundry.lock).
# Run `forge install` after a fresh clone to populate.
# Anchored to the foundry root so src/lib/ stays tracked.
/lib/

# Broadcast logs
broadcast/

# Local env
.env
