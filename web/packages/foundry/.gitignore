# Compiler artifacts
cache/
out/

# Foundry-installed dependencies (pinned via foundry.lock).
# Run `forge install` after a fresh clone to populate.
lib/

# Broadcast logs
broadcast/

# Local env
.env
